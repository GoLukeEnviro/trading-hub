# Repository Work Policy

Use a dedicated clean worktree. Preserve unrelated files. Resume existing work before creating duplicates. Keep commits issue-focused. Run the full validation suite before every push. Leave merge decisions to a human reviewer. Stop when dependencies or repository state are unclear.
