# START HERE — SI v2 Continuous Controller

Read the repository roadmap, `POLICY.md`, `STATE.json`, `QUEUE.json`, and `HANDOFF.md` in that order.

The first active task is reconciliation of the two overlapping planning pull requests. Continue only from the dependency-aware queue. Work in a dedicated worktree, keep each run bounded, run the full validation suite, update durable state after every run, and leave merges for human review.

The scheduler definitions in this branch are templates only and are not active.
