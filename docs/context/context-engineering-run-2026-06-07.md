# Context Engineering Intake Run — 2026-06-07

## What was promoted into specs
- `docs/specs/trading-system-audit-2026-06-07.md`
- `docs/specs/self-improvement-orchestrator-spec.md`
- `docs/specs/context-architecture.md`

## What changed conceptually
- `docs/specs/` is now the place for stable knowledge and reusable reference material.
- `docs/context/` is treated as the historical archive for audits, incident notes, and one-off run reports.
- Runtime noise paths are being pushed out of Git with explicit ignore rules.
- The self-improvement loop is documented as proposal-only, with persistent bot mapping and conservative deployment guards.

## What stays in context
- The June 5 comprehensive gap analysis and its backlog remain historical context.
- The May 31 root reality audit remains a useful forensic source.
- The May 11 fleet rescue report remains the comparative strategy baseline.
- The self-improvement readiness / improvement logs remain context because they explain the route to the current state.

## Open questions for a later run
1. Should the canonical trading status artifacts get a small spec/index page under `docs/specs/`?
2. Should the June 5 backlog be distilled into a dedicated backlog spec, or stay as history only?
3. Should additional self-improvement helper docs be promoted, or is the current spec layer enough?

## Notes
This file is intentionally a context artefact, not a spec. It exists to record the intake run while leaving the stable knowledge in `docs/specs/`.
