[pytest]
# Keep root test discovery narrow so repo-wide pytest does not collect unrelated subprojects.
testpaths = tests
markers =
    unit: fast isolated tests
    integration: local integration tests without live trading
    runtime: opt-in runtime tests requiring explicit environment
    docker: tests requiring Docker access
    slow: slower tests excluded from default fast runs
