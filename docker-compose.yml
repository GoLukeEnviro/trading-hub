services:
  docker-proxy:
    image: tecnativa/docker-socket-proxy:latest
    restart: unless-stopped
    environment:
      CONTAINERS: 1
      SERVICES: 1
      NETWORKS: 1
      INFO: 1
      EXEC: 1
      POST: 1
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
    - proxy-net
  trading-dashboard:
    build:
      context: ./docker/dashboard
      dockerfile: Dockerfile
    image: trading-dashboard:stable
    container_name: trading-dashboard
    restart: unless-stopped
    environment:
      DOCKER_HOST: tcp://docker-proxy:2375
    volumes:
    - ./dashboard.py:/app/dashboard.py:ro
    ports:
    - 127.0.0.1:5000:5000
    networks:
    - proxy-net
    - hermes-net
    depends_on:
    - docker-proxy
  ai-hedge-fund:
    image: trading-ai-hedge-fund-crypto:latest
    command:
    - python3
    - docker/service_entrypoint.py
    ports:
    - 127.0.0.1:8410:8080
    env_file:
    - ./ai-hedge-fund-crypto/.env
    environment:
    - SERVICE_PORT=8080
    volumes:
    - ./ai-hedge-fund-crypto/output:/app/output
    healthcheck:
      test:
      - CMD
      - python3
      - -c
      - import urllib.request, json; d=json.loads(urllib.request.urlopen('http://localhost:8080/health').read());
        exit(0 if d['status']=='ok' else 1)
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
    restart: unless-stopped
    networks:
    - hermes-net
    - proxy-net
  hermes-green:
    image: nousresearch/hermes-agent:latest
    tty: true
    stdin_open: true
    environment:
      DOCKER_HOST: tcp://docker-proxy:2375
      PUID: 10000
      PGID: 10000
      HERMES_DASHBOARD: 1
      HERMES_DASHBOARD_INSECURE: 1
      HERMES_DASHBOARD_TUI: 1
      HERMES_DASHBOARD_HOST: 0.0.0.0
      HERMES_DASHBOARD_PORT: 9119
    volumes:
    - /opt/hermes-green/config:/opt/data
    - /opt/hermes/ssh:/home/hermes/.ssh:ro
    - /home/hermes/projects:/home/hermes/projects:rw
    - /home/hermes/projects/trading/weatherhermes_persistent/weatherbot_master:/home/hermes/weatherbot_master:ro
    - /var/run/docker.sock:/var/run/docker.sock:ro
    - /opt/hermes-green/scripts/99-telegram-settle-delay:/etc/cont-init.d/99-telegram-settle-delay:ro
    # NOTE 2026-06-10: mem0/__init__.py explicit mount removed — file is delivered
    # via the /opt/hermes-green/config:/opt/data tree mount. Duplicate explicit
    # mount caused ambiguity on container recreate. Rollback: uncomment line below.
    # - /opt/hermes-green/config/plugins/memory/mem0/__init__.py:/opt/hermes/plugins/memory/mem0/__init__.py:ro
    restart: unless-stopped
    networks:
    - hermes-net
    - proxy-net
    - trading_hermes-net
    depends_on:
    - docker-proxy
    container_name: hermes-green
    # NOTE 2026-06-10: sleep infinity removed — Hermes uses its native
    # s6 entrypoint (main-wrapper.sh). Docker restart policy and SIGTERM
    # handling now work correctly. If Hermes fails to start, check:
    #   docker logs hermes-green --tail 50
    # Rollback: re-add `command: [sleep, infinity]` and recreate.
    env_file: /opt/hermes-green/.env
    ports:
    - 127.0.0.1:8083:9119
    # DEPRECATED 2026-06-09: :8642 orphaned — API lives on :8083/api/* (dashboard :9119)
    # - 127.0.0.1:8642:8642
    logging:
      driver: json-file
      options:
        max-size: 50m
        max-file: '3'
  green-qdrant:
    image: qdrant/qdrant:latest
    volumes:
    - green-qdrant-data:/qdrant/storage
    networks:
    - trading_hermes-net
    restart: unless-stopped
    container_name: green-qdrant
    logging:
      driver: json-file
      options:
        max-size: 20m
        max-file: '3'
  green-ollama:
    image: ollama/ollama:latest
    volumes:
    - green-ollama-data:/root/.ollama
    networks:
    - trading_hermes-net
    restart: unless-stopped
    container_name: green-ollama
    # Fix G-02: removed broken "command: [sleep, infinity]" which collided
    # with ollama entrypoint. Now runs default "ollama serve" for Mem0 embeddings.
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    logging:
      driver: json-file
      options:
        max-size: 20m
        max-file: '3'
  green-mem0:
    image: hermes-mem0-local-api:stable
    networks:
    - trading_hermes-net
    restart: unless-stopped
    depends_on:
    - green-qdrant
    - green-ollama
    container_name: green-mem0
    environment:
      QDRANT_HOST: green-qdrant
      QDRANT_PORT: 6333
      OLLAMA_BASE_URL: http://green-ollama:11434
      MEM0_LLM_BASE_URL: https://ollama.com/v1
      MEM0_LLM_MODEL: gemma3:27b
      MEM0_COLLECTION: hermes_memories_v3
      MEM0_EMBEDDER_MODEL: nomic-embed-text
      MEM0_EMBEDDING_DIMS: 768
      MEM0_API_HOST: 0.0.0.0
      MEM0_API_PORT: 8787
      OLLAMA_API_KEY: ${OLLAMA_API_KEY}
    env_file: /home/hermes/projects/trading/.env
    # Bind-mount: live patch of app.py + extraction policies (Dream Mode Recovery 2026-06-08).
    # Mounts override the in-image copies; rollback = remove these volumes + recreate.
    volumes:
    - /opt/data/local-memory/app/app.py:/app/app.py:ro
    - /opt/data/local-memory/app/extraction_policy_v2.txt:/app/extraction_policy_v2.txt:ro
    - /opt/data/local-memory/app/extraction_policy_v1.txt:/app/extraction_policy_v1.txt:ro
    ports:
    - 127.0.0.1:8788:8787
    logging:
      driver: json-file
      options:
        max-size: 20m
        max-file: '3'
  freqtrade-freqforge:
    image: freqtradeorg/freqtrade:stable
    ports:
    - 127.0.0.1:8086:8080
    command: trade --config /freqtrade/user_data/config.json --strategy FreqForge_Override
    environment:
    - FREQTRADE__TELEGRAM__ENABLED=false
    volumes:
    - ./freqforge/user_data:/freqtrade/user_data
    - ./freqtrade/shared:/freqtrade/shared
    - ./freqtrade/shared/primo_signal_state.json:/freqtrade/user_data/primo_signal_state.json:ro
    restart: unless-stopped
    mem_limit: 512m
    cpus: '1.0'
    # Healthcheck: unauthenticated /api/v1/ping on localhost only (issue #199)
    healthcheck:
      test: ["CMD", "curl", "-fsS", "--max-time", "5", "http://127.0.0.1:8080/api/v1/ping"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    networks:
    - hermes-net
  freqtrade-regime-hybrid:
    image: freqtradeorg/freqtrade:stable
    command: trade --config /freqtrade/user_data/config.json --strategy RegimeSwitchingHybrid_v7_v04_Integration
    ports:
    - 127.0.0.1:8085:8080
    environment:
    - FREQTRADE__TELEGRAM__ENABLED=false
    volumes:
    - ./freqtrade/bots/regime-hybrid/user_data:/freqtrade/user_data
    - ./freqtrade/shared:/freqtrade/shared
    - ./freqtrade/shared/primo_signal_state.json:/freqtrade/user_data/primo_signal_state.json:ro
    restart: unless-stopped
    mem_limit: 512m
    cpus: '1.0'
    # Healthcheck: unauthenticated /api/v1/ping on localhost only (issue #199)
    healthcheck:
      test: ["CMD", "curl", "-fsS", "--max-time", "5", "http://127.0.0.1:8080/api/v1/ping"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    networks:
    - hermes-net
  freqtrade-freqforge-canary:
    image: freqtradeorg/freqtrade:stable
    command: trade --config /freqtrade/user_data/config.json --strategy FreqForge_Override
    ports:
    - 127.0.0.1:8081:8080
    environment:
    - FREQTRADE__TELEGRAM__ENABLED=false
    volumes:
    - ./freqforge-canary/user_data:/freqtrade/user_data
    - ./freqtrade/shared:/freqtrade/shared
    - ./freqtrade/shared/primo_signal_state.json:/freqtrade/user_data/primo_signal_state.json:ro
    restart: unless-stopped
    mem_limit: 512m
    cpus: '0.5'
    # Healthcheck: unauthenticated /api/v1/ping on localhost only (issue #199)
    healthcheck:
      test: ["CMD", "curl", "-fsS", "--max-time", "5", "http://127.0.0.1:8080/api/v1/ping"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    networks:
    - hermes-net
  freqai-rebel:
    image: freqtrade-freqai-rebel:custom
    command: trade --config /freqtrade/user_data/config.json --strategy RebelLiquidation
    ports:
    - 127.0.0.1:8087:8080
    environment:
    - FREQTRADE__TELEGRAM__ENABLED=false
    volumes:
    - ./freqtrade/bots/freqai-rebel/user_data:/freqtrade/user_data
    - ./freqtrade/shared:/freqtrade/shared
    - ./freqtrade/shared/primo_signal_state.json:/freqtrade/user_data/primo_signal_state.json:ro
    restart: unless-stopped
    mem_limit: 2g
    cpus: '2.0'
    # Healthcheck: unauthenticated /api/v1/ping on localhost only (issue #199)
    healthcheck:
      test: ["CMD", "curl", "-fsS", "--max-time", "5", "http://127.0.0.1:8080/api/v1/ping"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    networks:
    - hermes-net
  freqtrade-webserver:
    image: freqtradeorg/freqtrade:stable
    ports:
    - 127.0.0.1:8180:8080
    command: webserver --config /freqtrade/user_data/config.json
    volumes:
    - ./freqforge/user_data:/freqtrade/user_data
    restart: unless-stopped
    mem_limit: 256m
    cpus: '0.5'
    # Healthcheck: unauthenticated /api/v1/ping on localhost only (issue #199)
    healthcheck:
      test: ["CMD", "curl", "-fsS", "--max-time", "5", "http://127.0.0.1:8080/api/v1/ping"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    networks:
    - hermes-net
  hermes-watchdog:
    image: alpine:latest
    command: |
      sh -c 'while true; do
        wget -qO- --timeout=5 http://trading-freqtrade-freqforge-1:8080/api/v1/ping || echo "ALERT: freqforge down at $(date)" >> /var/log/watchdog.log
        wget -qO- --timeout=5 http://trading-freqtrade-freqforge-canary-1:8080/api/v1/ping || echo "ALERT: freqforge-canary down at $(date)" >> /var/log/watchdog.log
        wget -qO- --timeout=5 http://trading-freqtrade-regime-hybrid-1:8080/api/v1/ping || echo "ALERT: regime-hybrid down at $(date)" >> /var/log/watchdog.log
        wget -qO- --timeout=5 http://trading-freqai-rebel-1:8080/api/v1/ping || echo "ALERT: rebel down at $(date)" >> /var/log/watchdog.log
        wget -qO- --timeout=5 http://trading-freqtrade-webserver-1:8080/api/v1/ping || echo "ALERT: webserver down at $(date)" >> /var/log/watchdog.log
        wget -qO- --timeout=5 http://trading-ai-hedge-fund-1:8080/health || echo "ALERT: ai-hedge-fund down at $(date)" >> /var/log/watchdog.log
        sleep 60
      done'
    volumes:
    - watchdog-logs:/var/log
    restart: unless-stopped
    networks:
    - hermes-net
  caddy:
    image: caddy:alpine
    network_mode: host
    volumes:
    - ./Caddyfile:/etc/caddy/Caddyfile:ro
    - caddy_data:/data
    - caddy_config:/config
    restart: unless-stopped
  shadowlock:
    build:
      context: ./shadowlock
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./var/trading-shadowlock:/app/var/trading-shadowlock
    environment:
      - POLL_INTERVAL_SECONDS=60
      - HEARTBEAT_INTERVAL_SECONDS=300
      - LOG_LEVEL=INFO
    healthcheck:
      test: >
        python -c "
        import os,json,glob,datetime;
        b=os.environ.get('SHADOWLOCK_BASE_DIR','/app/var/trading-shadowlock');
        n=datetime.datetime.utcnow();
        c=n-datetime.timedelta(seconds=600);
        d=os.path.join(b,'logs',str(n.year),f'{n.month:02d}');
        if not os.path.isdir(d): exit(1);
        fs=sorted(glob.glob(os.path.join(d,'*.jsonl')),reverse=True);
        if not fs: exit(1);
        for l in open(fs[0]):
            l=l.strip();
            if not l: continue;
            e=json.loads(l);
            if e.get('event_type')!='shadowlock_heartbeat': continue;
            t=e.get('timestamp_utc','');
            if t.endswith('Z') and datetime.datetime.strptime(t.replace('Z',''),'%Y-%m-%dT%H:%M:%S')>=c: exit(0);
        exit(1)
        "
      interval: 60s
      timeout: 5s
      retries: 3
    networks:
      - hermes-net
networks:
  hermes-net:
    driver: bridge
  proxy-net:
    driver: bridge
    internal: true
  trading_hermes-net:
    external: true
volumes:
  green-qdrant-data:
    external: true
  green-ollama-data:
    external: true
  freqai-rebel-data: null
  watchdog-logs: null
  caddy_data:
    external: true
  caddy_config:
    external: true
